caso seja preciso para dados para o bancos de dados.
cd restaurante-analytcs
cd backend
node populate.js

para iniciar o servidor 
node server.js

para abir o site, so abrir o index.html direto no navegador ou pode iniciar a pasta na web e ir a o arquivo index.html
